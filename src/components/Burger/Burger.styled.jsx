import styled from 'styled-components'

const zoneMenu = styled.div`
   @media only screen and (max-width: 900px) {
        position: fixed;
        height: 50px;
        z-index: 10;
    } 
`

export default zoneMenu